/* Fatorial recursivo */

int fatorial(int n){

    if(n <= 1){
        return(1);
    }
    else{
        return(n * fatorial(n - 1));
    }
}

void main(void){
    int n;
    n = input();
    output(fatorial(n));
}